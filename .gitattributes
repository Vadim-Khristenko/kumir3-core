# Golden snapshot fixtures are compared byte-for-byte by tests — pin to LF so
# autocrlf never rewrites them to CRLF in the working copy (caused CI/local drift).
shared/tests/golden/*.snapshot text eol=lf
