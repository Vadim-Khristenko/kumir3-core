# Run guide 

To run the program using the interpreter, just run the following command

```bash
cargo run --bin interpreter-cli path_to_the_run_program
```

or if you want a slightly more interesting output

```bash
cargo run --bin interpreter-tui path_to_the_run_program
 ```
